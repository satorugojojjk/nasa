{
  "name": "orbit-pulse",
  "version": "0.1.0",
  "private": true,
  "description": "A learning-focused real-time space data backend.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "fetch:iss": "tsx src/scripts/fetch-iss.ts",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/app.test.js dist/clients/celestrak.test.js dist/services/orbit-refresh.test.js dist/stores/orbit-store.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
